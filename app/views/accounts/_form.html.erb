<%= nested_form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <%= f.fields_for :people do |people_form| %>
    <%= people_form.text_field :name %>
    <%= people_form.link_to_remove "Remove this people" %>
  <% end %>
  <p><%= f.link_to_add "Add a people", :people %></p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
